services:
  - type: web
    name: smart-lender
    runtime: python
    rootDir: Flask
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
